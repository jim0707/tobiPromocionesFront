export function correoReducer(state={
    
	acceso: false
	},action)
	{
		 /*
		switch(action.type){
            case "ENVIAR_CORREO":
            return state;
           
				return {
					...state,
					referenciador: action.referenciador,
					acceso: action.acceso
				}
                break;
           
		}
		 */
		return state;
	}