export function cuponReducer(state={
    
	acceso: false
	},action)
	{
		/*
		switch(action.type){
            case "GENERAR_CUPON":
            return state;
            
				return {
					...state,
					referenciador: action.referenciador,
					acceso: action.acceso
				}
                break;
            
		}
		*/
		return state;
	}